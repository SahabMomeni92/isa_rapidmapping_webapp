from django.urls import path
from . import views
urlpatterns = [
    # path('/feedbackForm/',views.feedback,name='feedbackForm'),
    # path('/',views.manager_panelView.as_view(),name='manager_panel'),
    
] 

